# Non-secret Firebase Functions parameters for the staging project.
ENVIRONMENT=pilot
REGION=europe-west1
LOG_LEVEL=info
MAX_SEARCH_RADIUS_METERS=5000
TRIP_REQUEST_TIMEOUT_SECONDS=120
# Explicit staging sentinel: routing intentionally uses the documented mock fallback.
MAPBOX_ACCESS_TOKEN=your-mapbox-token-here
